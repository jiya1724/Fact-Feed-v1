# run pip install -r requirements.txt

Flask>=2.2
Flask-SQLAlchemy>=3.0
Flask-Migrate>=4.0
Flask-Cors>=4.0
feedparser>=6.0
requests>=2.28
APScheduler>=3.10
python-dotenv>=1.0
joblib>=1.3
scikit-learn>=1.3
nltk>=3.8
pandas>=2.0
numpy>=1.24
sumy>=0.11


# Flask, Flask-SQLAlchemy, Flask-Migrate, Flask-Cors: Your core backend stack.
# feedparser, requests: For fetching and parsing RSS and APIs.
# APScheduler: For scheduled fetching.
# python-dotenv: For environment variables (API keys).
# joblib, scikit-learn: For ML model loading and prediction.
# nltk, pandas, numpy: For NLP/ML preprocessing.
# sumy: For summarization.